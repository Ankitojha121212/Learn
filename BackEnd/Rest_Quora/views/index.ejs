<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FeedBack Hell</title>
    <link rel="stylesheet" href="style.css"/>
</head>
<body>
    <h1>FeedBack Hell!</h1>

    <% for(post of posts) {  %>
        <div>
            <h3>@<%= post.username %></h3>
            <h4> <%= post.content %></h4>
            <br>
            <a href="http://localhost:8080/posts/<%= post.id %>"><button>See more</button></a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit"><button>Edit Content</button></a>
            <form method="post" action="/posts/<%=post.id%>?_method=DELETE">
                <button>Delete post</button>

            </form>
        </div>

  <%  } %>

  <a href="http://localhost:8080/posts/new" ><button>Create new Post</button></a>
</body>
</html>